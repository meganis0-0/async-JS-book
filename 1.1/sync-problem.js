// ПРОБЛЕМА: если бы fetch работал синхронно
console.log("1. Показываем интерфейс");
console.log("2. Пользователь кликает кнопку 'Загрузить'");
// Представьте, что это синхронный запрос к серверу
function synchronousFetch(url) {
  console.log(`3. Отправляем запрос на ${url}`);
  // ⚠️ ПРОБЛЕМА: браузер "зависнет" на 2 секунды!
  heavyOperation("Сетевой запрос", 2000);
  console.log("4. Получили ответ от сервера");
  return "Данные с сервера";
}
const data = synchronousFetch("https://api.example.com/data");
// Всё это время пользователь не может:
// - прокручивать страницу
// - кликать по другим кнопкам
// - видеть анимации
console.log("5. Показываем данные:", data);
console.log("6. Только теперь интерфейс снова отвечает");
