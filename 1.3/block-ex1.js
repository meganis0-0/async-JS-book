console.log(" Начало программы");
// Блокирующая операция - цикл, который выполняется 3 секунды
function blockingOperation() {
  console.log(" Начало блокирующей операции...");
  const startTime = Date.now();
  while (Date.now() - startTime < 3000) {
    // Просто ждём 3 секунды - имитация тяжелой операции
  }
  console.log(" Блокирующая операция завершена");
}
// Вызываем блокирующую функцию
blockingOperation();
// Этот код выполнится только ЧЕРЕЗ 3 СЕКУНДЫ!
console.log(" Конец программы");
